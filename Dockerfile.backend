FROM node:20-alpine

WORKDIR /app

# Copy root package files
COPY package*.json ./

# Install ALL dependencies (including backend deps)
RUN npm install

# Copy backend source code
COPY vid_summ_back/ ./

# Expose the port
EXPOSE 5000

# Start the server
CMD ["node", "server.js"]